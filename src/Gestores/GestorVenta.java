package Gestores;

import java.util.ArrayList;
import java.util.Hashtable;
import java.util.List;

import Entidades.Estacion;
import Grafo.Grafo;
import Grafo.Vertice;

public class GestorVenta {

	/*public static <T> float menor_peso (Grafo<T> g, Estacion origen) {
		Hashtable<Vertice, Integer> visitar = new Hashtable();
		Hashtable<Vertice, Integer> pesoMin = new Hashtable();
		List<Vertice> visitado = new ArrayList();
		
		pesoMin.put(g.getNodo(origen), 0);
		for (int i=0; i<g.getVertices().size(); i++) {
			if (g.getVertices().get(i).getValue() != origen.getNombre() ) {
				pesoMin.put(g.getVertices().get(i), null);
			}
		}
		
		
		visitar.put(origen.getNombre(), 0);
		
		while (visitar.isEmpty() == false) {
			g.getNeighbourhood(valor)
		}
		
		return 0;
	} */
	
		
	

}
